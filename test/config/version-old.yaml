schemaVersion: "2.0.0"

# Applied only to SBT 0.12 tags. 0.13 renamed the task to `sbtVersion`
# (camelCase); see version.yaml.

commandTests:
  - name: "sbt sbt-version prints the SBT version"
    command: "sbt"
    args: ["sbt-version"]
    expectedOutput: ["0\\.12\\.[0-9]+"]
